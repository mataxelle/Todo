{% extends 'base.html.twig' %}

{% block body %}

    {% if app.user %}
   <div class="row">
    <div class="col">
    <a href="{{ path('task_create') }}" class="btn btn-success mb-2">Créer une nouvelle tâche</a>
        <a href="{{ path('task_list', {'id': app.user.id}) }}" class="btn btn-info mb-2">Consulter la liste des tâches à faire</a>
        <a href="{{ path('done_task_list', {'id': app.user.id}) }}" class="btn btn-secondary mb-2">Consulter la liste des tâches terminées</a>
    </div>
   </div>
    {% endif %}
    
{% endblock %}
